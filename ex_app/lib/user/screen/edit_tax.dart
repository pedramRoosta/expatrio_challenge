import 'package:flutter/material.dart';

class EditTaxScreen extends StatelessWidget {
  const EditTaxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Edit tax screen'),
    );
  }
}
